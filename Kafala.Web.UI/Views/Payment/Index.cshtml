@using System.Web.UI.HtmlControls
@using Foundation.Web.Extensions
@model Kafala.Web.ViewModels.Payment.PaymentIndexViewModel

<p>
    @Html.ActionLink("Create New", "Create")
</p>
    <table class="table sorter">
        <tr>
            @Html.SortableHeader(Model.PagingInfo.Sort, "DonationCaseName", "Donation Case Name", Model.PagingInfo.ActionFunc)
            @Html.SortableHeader(Model.PagingInfo.Sort, "DonorName", "Donor Name", Model.PagingInfo.ActionFunc)
            @Html.SortableHeader(Model.PagingInfo.Sort, "PaymentDate", "Payment Date", Model.PagingInfo.ActionFunc)
            @Html.SortableHeader(Model.PagingInfo.Sort, "PaymentPeriod", "Payment Period", Model.PagingInfo.ActionFunc)
            <th></th>
        </tr>
    
        @foreach (var item in Model.Payments)
        {
          
        }
    
    </table>
@Html.PageLinks(Model.PagingInfo, Model.PagingInfo.ActionFunc)