@using System.Web.UI.HtmlControls
@using Foundation.Web.Extensions
@using Foundation.Web.Paging
@using Foundation.Web.Sorter
@model Kafala.Web.ViewModels.Payment.PaymentIndexViewModel

<div class="panel panel-primary">

    @Html.Partial("Partial/PaymentFilter", Model.PaymentFilter)
</div>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
    <table class="table sorter">
        <tr>
            @Html.SortableHeader(Model.PaymentFilter.Sort, Model.PaymentFilter.SortDirection, "DonationCaseName", "Donation Case Name", Model.PaymentFilter.ActionFunc)
            @Html.SortableHeader(Model.PaymentFilter.Sort, Model.PaymentFilter.SortDirection, "DonorName", "Donor Name", Model.PaymentFilter.ActionFunc)
            @Html.SortableHeader(Model.PaymentFilter.Sort, Model.PaymentFilter.SortDirection, "PaymentDate", "Payment Date", Model.PaymentFilter.ActionFunc)
            @Html.SortableHeader(Model.PaymentFilter.Sort, Model.PaymentFilter.SortDirection, "PaymentPeriod", "Payment Period", Model.PaymentFilter.ActionFunc)
            <th></th>
        </tr>
    
        @foreach (var item in Model.Payments)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.CommitmentDonationCaseName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CommitmentDonorName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PaymentDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PaymentPeriodName)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new {id = item.Id}) |
                    @Html.ActionLink("Details", "Details", new {id = item.Id}) |
                    @Html.ActionLink("Delete", "Delete", new {id = item.Id})
                </td>
            </tr>
        }
    
    </table>
@Html.PageLinks(Model.PaymentFilter, Model.PaymentFilter.ActionFunc)