@model Kafala.Web.ViewModels.DonationCase.ViewDonationCaseViewModel

<h2>Edit Donation Case</h2>
@using (var form = Html.Bootstrap().BeginForm(BootstrapFormType.Horizontal, new {@class= "well" ,id = "autoForm" }))
{
    @Html.DynamicView()
   
}

 <div class="row">
            
            @if (Model.Commitments != null)
            {
                @Html.Partial("Partial/Commitments", Model, new ViewDataDictionary())
            }
        </div>