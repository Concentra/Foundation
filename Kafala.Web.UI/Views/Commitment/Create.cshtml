@model Kafala.Web.ViewModels.Commitment.CreateCommitmentViewModel

<h2>Associate Donor To Case</h2>
@using (var form = Html.Bootstrap().BeginForm(BootstrapFormType.Horizontal, new {@class= "well" ,id = "autoForm" }))
{
    @Html.FormBuilder().DynamicForm();
}
