@using Foundation.Web.Extensions
@model Kafala.Web.ViewModels.Commitment.CommitmentIndexViewModel

<h2>List Commitments</h2>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table sorter">
        <tr>
           @Html.SortableHeader(Model.Search.PagingInfo.Sort, Model.Search.PagingInfo.SortDirection,  "DonationCaseName" , "Donation Case" , Model.Search.PagingInfo.ActionFunc)
            @Html.SortableHeader(Model.Search.PagingInfo.Sort, Model.Search.PagingInfo.SortDirection, "Name" , "Donor" , Model.Search.PagingInfo.ActionFunc)
            @Html.SortableHeader(Model.Search.PagingInfo.Sort, Model.Search.PagingInfo.SortDirection, "Start Date" , "Start Date" , Model.Search.PagingInfo.ActionFunc)
            @Html.SortableHeader(Model.Search.PagingInfo.Sort, Model.Search.PagingInfo.SortDirection, "End Date" , "End Date" , Model.Search.PagingInfo.ActionFunc)
        </tr>
    
    @foreach (var item in Model.Commitments) {
        <tr>
            <td>
                @item.DonationCaseName
            </td>
            <td>
                @item.DonorName
            </td>
            <td>
                @item.StartDate
            </td>
            <td>
                @item.EndDate
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }
    
    </table>
@Html.PageLinks(Model.Search.PagingInfo, Model.Search.PagingInfo.ActionFunc)