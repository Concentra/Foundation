@model Kafala.Web.ViewModels.Donor.DonorCreateViewModel


@using (var form = Html.Bootstrap().BeginForm(BootstrapFormType.Horizontal, new {id = "autoForm" }))
{
    @Html.Bootstrap().DynamicForm(BootstrapFormType.Horizontal);
}

