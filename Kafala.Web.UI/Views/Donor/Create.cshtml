@using Foundation.FormBuilder;
@model Kafala.Web.ViewModels.Donor.CreateDonorViewModel


<h2>Register a new donor</h2>
@using (var form = Html.Bootstrap().BeginForm(BootstrapFormType.Vertical, new {@class= "well" ,id = "autoForm" }))
{
    @Html.FormBuilder().DynamicForm(Foundation.FormBuilder)
   
}

